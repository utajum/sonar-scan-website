<!-- Usage: animated terminal cards showing each CLI command -->
<section class="section">
  <div class="container">
    <h2>Usage</h2>
    <p class="section-desc">Every command you need, nothing you don't. Run <code class="inline-code">sonar-scan</code> with a flag or let the interactive menu guide you.</p>

    <div class="usage-grid">
      <div class="usage-card">
        <div class="usage-desc">
          <h3>Interactive menu</h3>
          <p>Run without arguments for a retro-styled terminal menu.</p>
        </div>
        <div class="usage-terminal" data-command="sonar-scan">
          <div class="ut-bar"><span class="ut-dots"><i></i><i></i><i></i></span></div>
          <div class="ut-body"><code><span class="prompt">$ </span><span class="typed-cmd"></span><span class="typed-comment"></span><span class="ut-cursor">_</span></code></div>
        </div>
      </div>

      <div class="usage-card">
        <div class="usage-desc">
          <h3>Run a scan</h3>
          <p>Scan the current directory for vulnerabilities.</p>
        </div>
        <div class="usage-terminal" data-command="sonar-scan --scan" data-comment="# or -s">
          <div class="ut-bar"><span class="ut-dots"><i></i><i></i><i></i></span></div>
          <div class="ut-body"><code><span class="prompt">$ </span><span class="typed-cmd"></span><span class="typed-comment"></span><span class="ut-cursor">_</span></code></div>
        </div>
      </div>

      <div class="usage-card">
        <div class="usage-desc">
          <h3>Scan specific directory</h3>
          <p>Point it at any project path on your system.</p>
        </div>
        <div class="usage-terminal" data-command="sonar-scan /path/to/project" data-comment="# or -t /path">
          <div class="ut-bar"><span class="ut-dots"><i></i><i></i><i></i></span></div>
          <div class="ut-body"><code><span class="prompt">$ </span><span class="typed-cmd"></span><span class="typed-comment"></span><span class="ut-cursor">_</span></code></div>
        </div>
      </div>

      <div class="usage-card">
        <div class="usage-desc">
          <h3>Download issues</h3>
          <p>Export all detected issues to a structured JSON file.</p>
        </div>
        <div class="usage-terminal" data-command="sonar-scan --download" data-comment="# or -d">
          <div class="ut-bar"><span class="ut-dots"><i></i><i></i><i></i></span></div>
          <div class="ut-body"><code><span class="prompt">$ </span><span class="typed-cmd"></span><span class="typed-comment"></span><span class="ut-cursor">_</span></code></div>
        </div>
      </div>

      <div class="usage-card">
        <div class="usage-desc">
          <h3>Quiet mode</h3>
          <p>Minimal output. Perfect for CI/CD pipelines.</p>
        </div>
        <div class="usage-terminal" data-command="sonar-scan --quiet" data-comment="# or -q">
          <div class="ut-bar"><span class="ut-dots"><i></i><i></i><i></i></span></div>
          <div class="ut-body"><code><span class="prompt">$ </span><span class="typed-cmd"></span><span class="typed-comment"></span><span class="ut-cursor">_</span></code></div>
        </div>
      </div>

      <div class="usage-card">
        <div class="usage-desc">
          <h3>Clean up Docker</h3>
          <p>Remove SonarQube and scanner Docker images.</p>
        </div>
        <div class="usage-terminal" data-command="sonar-scan --delete-images">
          <div class="ut-bar"><span class="ut-dots"><i></i><i></i><i></i></span></div>
          <div class="ut-body"><code><span class="prompt">$ </span><span class="typed-cmd"></span><span class="typed-comment"></span><span class="ut-cursor">_</span></code></div>
        </div>
      </div>

      <div class="usage-card">
        <div class="usage-desc">
          <h3>Help</h3>
          <p>Display all available commands and options.</p>
        </div>
        <div class="usage-terminal" data-command="sonar-scan --help" data-comment="# or -h">
          <div class="ut-bar"><span class="ut-dots"><i></i><i></i><i></i></span></div>
          <div class="ut-body"><code><span class="prompt">$ </span><span class="typed-cmd"></span><span class="typed-comment"></span><span class="ut-cursor">_</span></code></div>
        </div>
      </div>

      <div class="usage-card">
        <div class="usage-desc">
          <h3>Version</h3>
          <p>Print the currently installed version.</p>
        </div>
        <div class="usage-terminal" data-command="sonar-scan --version" data-comment="# or -v">
          <div class="ut-bar"><span class="ut-dots"><i></i><i></i><i></i></span></div>
          <div class="ut-body"><code><span class="prompt">$ </span><span class="typed-cmd"></span><span class="typed-comment"></span><span class="ut-cursor">_</span></code></div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // Usage terminal typing animations
  function typeCommand(el: HTMLElement, command: string, comment: string) {
    const cmdSpan = el.querySelector('.typed-cmd') as HTMLElement;
    const commentSpan = el.querySelector('.typed-comment') as HTMLElement;
    const cursorSpan = el.querySelector('.ut-cursor') as HTMLElement;
    if (!cmdSpan || !cursorSpan) return;

    let i = 0;
    let j = 0;
    cursorSpan.classList.remove('blink');

    function typeComment() {
      if (!comment || !commentSpan) {
        cursorSpan.classList.add('blink');
        return;
      }
      if (j < comment.length) {
        commentSpan.textContent = comment.slice(0, j + 1);
        j++;
        setTimeout(typeComment, 25 + Math.random() * 20);
      } else {
        cursorSpan.classList.add('blink');
      }
    }

    function tick() {
      if (i < command.length) {
        cmdSpan.textContent = command.slice(0, i + 1);
        i++;
        setTimeout(tick, 35 + Math.random() * 40);
      } else {
        if (comment) {
          cmdSpan.textContent = command + '  ';
          setTimeout(typeComment, 300);
        } else {
          cursorSpan.classList.add('blink');
        }
      }
    }
    tick();
  }

  const usageTerminals = document.querySelectorAll('.usage-terminal');
  const usageObserver = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        usageObserver.unobserve(entry.target);
        const el = entry.target as HTMLElement;
        const command = el.dataset.command || '';
        const comment = el.dataset.comment || '';
        const allTerminals = Array.from(usageTerminals);
        const idx = allTerminals.indexOf(el);
        setTimeout(() => {
          typeCommand(el, command, comment);
        }, idx * 150);
      }
    });
  }, { threshold: 0.3 });

  usageTerminals.forEach((t) => usageObserver.observe(t));
</script>

<style>
  .inline-code {
    font-size: 0.9rem;
    background: rgba(0, 255, 255, 0.1);
    padding: 0.15rem 0.45rem;
    border-radius: 4px;
    color: var(--neon-cyan);
    font-family: 'JetBrains Mono', 'SF Mono', 'Fira Code', Consolas, monospace;
  }

  .usage-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.25rem;
  }

  .usage-card {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    padding: 1.25rem;
    background: var(--bg-card);
    border-radius: var(--radius-sm);
    border: 1px solid var(--border-dark);
    transition: border-color 0.2s, box-shadow 0.2s;
  }

  .usage-card:hover {
    border-color: var(--neon-cyan);
    box-shadow: 0 0 15px rgba(0, 255, 255, 0.08);
  }

  .usage-desc h3 {
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--text-primary);
    margin: 0 0 0.25rem;
  }

  .usage-desc p {
    font-size: 0.84rem;
    color: var(--text-muted);
    margin: 0;
    line-height: 1.45;
  }

  .usage-terminal {
    border-radius: 6px;
    overflow: hidden;
    background: #0a0d12;
    border: 1px solid rgba(255, 255, 255, 0.06);
  }

  .ut-bar {
    display: flex;
    align-items: center;
    padding: 0.4rem 0.75rem;
    background: #161b22;
  }

  .ut-dots {
    display: flex;
    gap: 5px;
  }

  .ut-dots i {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    display: block;
  }

  .ut-dots i:nth-child(1) { background: #ff5f57; }
  .ut-dots i:nth-child(2) { background: #febc2e; }
  .ut-dots i:nth-child(3) { background: #28c840; }

  .ut-body {
    padding: 0.75rem 1rem;
  }

  .ut-body code {
    font-family: 'JetBrains Mono', 'SF Mono', 'Fira Code', Consolas, monospace;
    font-size: 0.82rem;
    color: var(--text-primary);
    white-space: nowrap;
  }

  .prompt {
    color: var(--neon-green);
    margin-right: 0.5rem;
    user-select: none;
  }

  .ut-cursor {
    color: var(--neon-green);
    font-weight: 400;
  }

  .ut-cursor.blink {
    animation: blink-fade 1s step-end infinite;
  }

  @keyframes blink-fade {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  .typed-cmd {
    color: var(--neon-cyan);
  }

  .typed-comment {
    color: var(--text-dim);
    opacity: 0.5;
  }

  @media (max-width: 820px) {
    .usage-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
